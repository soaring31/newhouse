{% extends 'MemberBundle::base_manage_block.html.twig' %}

{% set models = app.request.get('models') %}
{% set category = app.request.get('category') %}
    {% set aid = app.request.get('aid') %}
{% block js %}
<script type="text/javascript">
    
</script>
{% endblock %}

{% block mainMenu %}
    {{ parent() }} 
    {{ core_manage.jump({
        title: '<i class="mr5"></i>发送列表',
        url: 'musersmslist/musersmssend'|U
    }) }}
    {{ core_manage.jump({
        title: '<i class="mr5"></i>发送站内短信',
        url: 'musersmslist/sendsms'|U
    }) }}
{% endblock %}

{% block mainleft %}
  {#   {{ render(controller('HouseBundle:Mnews:category', {category: category, action: 'mnews/mnewsmanage'})) }} #}
{% endblock mainleft %}

{% block tableMain %}
{{ core_manage.filter([
    
]) }} 
{# {{ core_manage.filter([
     {
        method: 'show',
        title: '<i class="font-ico-plus mr5"></i>发送短信'
    },
]) }} #}
<section class="list-con" data-param="{
        defUrl    : '{{ (action)|U }}',
        showUrl   : '{{ (action ~ '/show')|U }}',
        submitUrl : '{{ (action ~ '/save')|U }}',
        deleteUrl : '{{ (action ~ '/delete')|U }}',
        fixed     : '.filter',
        list      : '.list-con',
        data      : {
            models: '{{ models }}',
            category: '{{ category }}'
        }
    }">
    <div class="list">
        <table class="right-table table-hover">
            <tbody>
                <tr data-id>
                    <th width="40" class="tac">{{core_manage.checkbox()}}</th>
                    <th width="200">标题</th>
                    <th width="120">发信人</th>
                    <th width="120">发送日期</th>
                    <th>内容</th>
                </tr>
                {% if ident.info.data is defined and ident.info.pageCount|default(0) != 0 %}
                {% for k, vo in ident.info.data %}
                <tr data-id="{{ vo.id }}">
                    <td class="tac">{{core_manage.checkbox()}}</td>
                    <td>{{ vo.title }}</td>
                    <td >{{ vo.name }}</td>
                    <td >{{ vo.create_time|default('0')|date('Y-m-d') }}</td>
                    <td >{{ vo.content }}</td>
                    
                    
                </tr>
                {% endfor %}
                {% else %}
                <tr><td colspan="10" class="tac">没有数据！</td></tr>
                {% endif %}
            </tbody>
        </table>
    </div>
    {{core_manage.page(ident.info)}}
</section>

{{ core_manage.oprate([
    {
        method: 'delete',
        title: ' <i class="font-ico-del mr5"></i>批量删除',
    }
]) }}
{% endblock %}

{% block promptUl %}
{# 提示说明 #}
{% endblock %}




