{% extends 'HouseBundle::base_manage_block.html.twig' %}
{% block js %}
<script type="text/javascript">

</script>
{% endblock %}

{% block mainMenu %}
    {{ parent() }}
{% endblock %}

{% block mainleft %}
{% endblock mainleft %}

{% block tableMain %}
{{ core_manage.filter([
    {
        title: '发布时间',
        attr: {
            'data-selectdate': {
                unix: 1
            }
        },
        data: [
                {
                    id: '1M',
                    name: '一个月前'
                },
                {
                    id: '3M',
                    name: '三个月前'
                }
            ],
        field: 'create_time',
        method: 'selectFilter'
    },
    {
        method: 'search',
        field: 'name',
        title: '请搜索电话',
    }
]) }}
{# <input type="button" id="btnfun_delete_batch1" class="search-btn" data-url="{{ ('smslogs/delbatch')|U('kid=3') }}" value="删除3月前">
<input type="button" id="btnfun_delete_batch1" class="search-btn" data-url="{{ ('smslogs/delbatch')|U('kid=1') }}" value="删除1月前"> #}
<section class="list-con" data-param="{
    defUrl    : '{{ (action)|U }}',
    showUrl   : '{{ (action ~ '/show')|U }}',
    submitUrl : '{{ (action ~ '/save')|U }}',
    deleteUrl : '{{ (action ~ '/delete')|U }}',
    fixed     : '.filter',
    list      : '.list-con',
    data      : {
    }
}">
    <div class="list">
        <table class="right-table table-hover">
            <tbody>
                <tr data-id>
                    <th width="60" class="tac">{{core_manage.checkbox()}}</th>
                    <th width="60" data-role="sort" data-sort="id" class="sort">序号</th>
                    <th width="150">电话</th>
                    <th>验证码</th>
                    {# <th>信息内容</th> #}
                    <th width="200">模版名称</th>
                    <th width="100" data-role="sort" data-sort="create_time" class="sort">发送时间</th>
                    <th width="80" class="tac">操作</th>
                </tr>
                {% if ident.info.data is defined and ident.info.data|default('') %}
                {% for k, vo in ident.info.data %}
                <tr data-id="{{ vo.id }}">
                    <td class="tac">{{core_manage.checkbox()}}</td>
                    <td>{{ vo.id|default('') }}</td>
                    <td>{{ vo.name|default('') }}</td>
                    <td>{{ vo.code|default('') }}</td>
                    {# <td>[<a href="{{'msglog/preview'|U({id: vo.id})}}" data-role="show">预览</a>]</td> #}
                    <td>
                    {% set data = vo.content|json_decode() %}
                       {% set datab = ('db.smstpls')|getOne('name',{tid:data.tpl}) %}
                        {{datab|default('')}}
                    </td>
                    <td>{{ vo.create_time|default('')|date('Y-m-d') }}</td>
                    <td class="tac">
                        {{ core_manage.delete() }}
                    </td>
                </tr>
                {% endfor %}
                {% else %}
                <tr><td colspan="9" class="tac">没有数据！</td></tr>
                {% endif %}
            </tbody>
        </table>
    </div>
    {{core_manage.page(ident.info)}}
</section>

{{ core_manage.oprate([
    {
        method: 'delete',
        title: ' <i class="font-ico-del mr5"></i>批量删除',
    }
]) }}
{# ,
{
    method: 'btn',
    url: ("smslogs/delbatch")|U({kid: 3}),
    type: 'submit',
    attr: 'id="btnfun_delete_batch1"',
    class: 'btn-danger',
    title: ' <i class="font-ico-del mr5"></i>删除3月前',
},
{
    method: 'btn',
    url: ("smslogs/delbatch")|U({kid: 1}),
    attr: 'id="btnfun_delete_batch1"',
    class: 'btn-danger',
    title: ' <i class="font-ico-del mr5"></i>删除1月前',
} #}
{% endblock %}

{% block promptUl %}
<li>
    <b>结果错误代码对照表：</b>
    {% for key, msg in errInfo %}
        {{key}} = {{msg}};　 
    {% endfor %}
</li>
{% endblock %}




